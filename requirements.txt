# Pipeline (required — clean.py through export_dashboard_data.py)
pandas==2.2.3
numpy==2.1.3
PyYAML==6.0.3          # normalize.py reads categories.yaml + the overlay
openpyxl==3.1.5        # pandas.read_excel backend for the BLS .xlsx benchmark

# Collection (used by test_setup.py's environment check)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Exploration only — not imported by any pipeline stage
matplotlib==3.9.2
jupyter==1.1.1
